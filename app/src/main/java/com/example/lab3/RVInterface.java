package com.example.lab3;

// contract between any class that implements it and make sure that class has all the methods
// listed within our interface
public interface RVInterface {

    void onItemClick(int position);

}
